table! {
    org (id) {
        id -> Integer,
    }
}

table! {
    repo (id) {
        id -> Integer,
    }
}

table! {
    secret (id) {
        id -> Integer,
    }
}

table! {
    notify (id) {
        id -> Integer,
    }
}

table! {
    poll_manifest (id) {
        id -> Integer,
    }
}

table! {
    build_target (id) {
        id -> Integer,
    }
}

table! {
    build_summary (id) {
        id -> Integer,
    }
}

table! {
    build_stage (id) {
        id -> Integer,
    }
}
