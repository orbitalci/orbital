image: test
docker_packages: # apt-get install
  - maven<v>
  - blah
before_stages:
  script:
    - sh echo "hello"
after_stages:
  env:
    - BUILD_DEBUG=0
  script:
    - sh echo "goodbye"

build: 
  env:
    - BUILD_DEBUG=1
    - SEARCH_URL=https://google.com
  script:
    - echo "hello1"
test:
  script:
    - echo "goodbye"
deploy:
  script:
    - echo "fly away"