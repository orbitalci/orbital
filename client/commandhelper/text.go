package commandhelper

const (
	AcctRepoHelp = `If this command is run from within the cloned repo directory, ocelot will run a git command to detect the account and repository name from the origin url, so the -acct-repo flag is not necessary.
`
)