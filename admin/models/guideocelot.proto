syntax = "proto3";

package models;

import "google/protobuf/empty.proto";

service GuideOcelot {
    rpc GetCreds(google.protobuf.Empty) returns (CredWrapper) {}
}

message CredWrapper {
    repeated Credentials credentials = 1;
}

message Credentials {
    string clientId = 1;
    string clientSecret = 2;
    string tokenURL = 3;
    string acctName = 4;
    string type = 5;
}